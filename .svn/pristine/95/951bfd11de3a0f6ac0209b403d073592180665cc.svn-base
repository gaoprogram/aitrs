import * as types from '../mutation-types'

const salary = {
  state: {
    schemeCode: ''
  },
  mutations: {
    [types.SET_SCHEME_CODE] (state, schemeCode) {
      state.schemeCode = schemeCode
    }
  }
}

export default salary
